[
  {
    "id": "tool-weather-select",
    "category": "tool-selection",
    "prompt": "What is the current temperature in Paris, France? Use the available tool.",
    "tools": [
      {
        "type": "function",
        "function": {
          "name": "get_weather",
          "description": "Get the current weather for a location.",
          "parameters": {
            "type": "object",
            "properties": {
              "location": {"type": "string", "description": "City and country"}
            },
            "required": ["location"]
          }
        }
      },
      {
        "type": "function",
        "function": {
          "name": "get_stock_price",
          "description": "Get the current price of a stock ticker.",
          "parameters": {
            "type": "object",
            "properties": {"ticker": {"type": "string"}},
            "required": ["ticker"]
          }
        }
      }
    ],
    "expected_tool": "get_weather",
    "expected_arguments_contains": ["Paris"],
    "mock_tool": "get_weather",
    "mock_arguments": "{\"location\": \"Paris, France\"}"
  },
  {
    "id": "tool-search-select",
    "category": "tool-selection",
    "prompt": "Find recent news articles about the James Webb Space Telescope.",
    "tools": [
      {
        "type": "function",
        "function": {
          "name": "web_search",
          "description": "Search the web for a query.",
          "parameters": {
            "type": "object",
            "properties": {"query": {"type": "string"}},
            "required": ["query"]
          }
        }
      },
      {
        "type": "function",
        "function": {
          "name": "send_email",
          "description": "Send an email message.",
          "parameters": {
            "type": "object",
            "properties": {"to": {"type": "string"}, "body": {"type": "string"}},
            "required": ["to", "body"]
          }
        }
      }
    ],
    "expected_tool": "web_search",
    "expected_arguments_contains": ["Webb"],
    "mock_tool": "web_search",
    "mock_arguments": "{\"query\": \"James Webb Space Telescope news\"}"
  },
  {
    "id": "tool-calculator-args",
    "category": "argument-extraction",
    "prompt": "Multiply 7 by 6 using the calculator tool.",
    "tools": [
      {
        "type": "function",
        "function": {
          "name": "calculate",
          "description": "Evaluate an arithmetic expression.",
          "parameters": {
            "type": "object",
            "properties": {"expression": {"type": "string"}},
            "required": ["expression"]
          }
        }
      }
    ],
    "expected_tool": "calculate",
    "expected_arguments_contains": ["7", "6"],
    "mock_tool": "calculate",
    "mock_arguments": "{\"expression\": \"7 * 6\"}"
  }
]
